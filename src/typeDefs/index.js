/* ========================================================================== *\
	TYPE DEFINITIONS
\* ========================================================================== */
/**
 * Options to control the output of a delta calculation.
 *
 * @typedef {Object} TimeSpanResultOptions
 *
 * @property {boolean} [absolute=true] Returns the delta as a positive number.
 * @property {boolean} [rounded=true] Returns the result as an integer.
 *
 * @since {next}
 */


/**
 * Represents a time interval.
 *
 * @typedef {Object} TimeSpan
 *
 * @property {number} days The number days passed between two dates
 * @property {number} hours The number of hours of the non completed day which
 * @property {number} minutes
 * @property {number} seconds
 * @property {number} milliseconds
 */
export default {};
