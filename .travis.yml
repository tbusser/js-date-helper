language: node_js
node_js:
  - "lts/*"

before_deploy: |
function npm_dist_tag() {
    if [[ "$TRAVIS_TAG" = *"-"* ]]; then
      echo "next"
    else
      echo "latest"
    fi
  }

jobs:
  include:
    - stage: npm release
      if: tag IS present
      node_js: "lts/*"
      tag: $(npm_tag)
      script: ./scripts/build.sh
      before_deploy:
        cd ./tmp/package
      deploy:
        provider: npm
        email: "$NPM_EMAIL"
        api_key: "$NPM_TOKEN"
        skip_cleanup: true
        on:
          tags: true
